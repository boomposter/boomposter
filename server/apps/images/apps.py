from django.apps import AppConfig


class ImagesConfig(AppConfig):
    """Application config for images."""

    name = "apps.images"
    verbose_name = "Advertisement space images"
    label = "images"
